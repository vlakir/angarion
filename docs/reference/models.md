# Доменные модели

Сущности ядра: входящая запись `Record` (форма, которую видит
процессор и шаблоны), результат обработки `ProcessingResult`, исходящая
запись `OutboundRecord` и сопутствующие value-объекты (§4 ТЗ).

::: angarion.domain.models
