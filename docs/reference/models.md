# Доменные модели

Сущности ядра: входящее событие `InboundEvent` (форма, которую видит
процессор и шаблоны), результат обработки `ProcessingResult`, исходящее
сообщение и сопутствующие value-объекты (§4 ТЗ).

::: angarion.domain.models
