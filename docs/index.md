# angarion

Универсальная Python-библиотека для построения конвейеров обработки
**событий сообщений**:

```text
N источников → нормализация в события → очередь → обработка → M получателей
                                            ↘ аналитика в БД
```

Событие — первоклассная сущность: **новое сообщение** (включая ответы),
**редактирование**, **удаление**. Правки и удаления отслеживаются
надёжно — в том числе случившиеся за время простоя приложения (catch-up
по реестру сообщений с историей версий).

!!! warning "Статус: pre-1.0"
    Публичный API нестабилен до версии 1.0 — следите за
    [CHANGELOG](https://github.com/vlakir/angarion/blob/main/CHANGELOG.md).
    Полное техническое задание —
    [CONCEPT.md](https://github.com/vlakir/angarion/blob/main/CONCEPT.md)
    (immutable, v2.1).

## Ключевые идеи

- **Гексагональная архитектура (ports & adapters).** Ядро не зависит от
  Telegram, очереди, СУБД и веб-фреймворка. Замена мессенджера, очереди
  или хранилища — добавление адаптера, без изменения ядра.
- **At-least-once + идемпотентность** на входе (дедупликация событий) и
  на выходе (идемпотентные ключи доставки) — дубли гасятся, доставка не
  теряется, включая рестарты и повторные catch-up.
- **Catch-up после простоя** по курсорам и реестру сообщений.
- **Multicast-пайплайны**: один источник питает несколько независимых
  конвейеров с изолированными ретраями и DLQ.
- **Процессоры-плагины**: пользовательская логика (включая LLM) с
  опциональным персистентным состоянием; регистрация через entry points.
- **Web API + Web UI** (FastAPI, SSR на Jinja2 + htmx): диагностика,
  журнал аналитики, граф топологии, административные операции; расширяется
  пользовательскими ручками и страницами через DI поверх портов.
- **Первый адаптер — Telegram** (Telethon, пользовательские аккаунты,
  MTProto).

## Куда дальше

- [Быстрый старт](quick-start.md) — установка и первый запуск конвейера.
- [Деплой](guides/deploy.md) — systemd, бэкапы, reverse proxy, роли процессов.
- [Автору плагина](guides/plugin-authoring.md) — свой адаптер/процессор,
  сертификация контрактными тестами `angarion.testing`.
- [Web API и UI](guides/web-api.md) — запуск API, своя ручка и страница.
- [Справочник API](reference/index.md) — автодокументация публичных
  модулей.
